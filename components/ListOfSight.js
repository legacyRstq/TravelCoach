import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const ListOfSight = () => {
    return (
        <View style = {styles.container}>
            <Text></Text>
        </View>
    );
}

const styles = StyleSheet.create({
    container:{
        backgroundColor: 'blue'
    }
})

export default ListOfSight;